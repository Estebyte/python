# Para comentar bloques de codigo se usa el comando Ctrl + }

# Para escribir variables facilmente se puede escribir "f" y {variable}. ej:
# a = int(input("Write any number: "))
# print(f"Your number is {a}")

# Para ir cambiando el valor de una variable es posible escribir:
# var += 1 en vez de var = var + 1

# Para unir los caracteres de una lista en un str :
# lista = "".join(lista1)
